{
  "private": true,
  "name": "@arca/desktop",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "contract:check": "node scripts/generate-ipc-contract-fixtures.mjs --check",
    "contract:generate": "node scripts/generate-ipc-contract-fixtures.mjs",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "lint": "pnpm typecheck",
    "test": "vitest run",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tauri-apps/cli": "^2",
    "@types/node": "^26",
    "svelte": "^5.56.10",
    "svelte-check": "^4",
    "typescript": "^6",
    "vite": "^8",
    "vitest": "^4"
  }
}
